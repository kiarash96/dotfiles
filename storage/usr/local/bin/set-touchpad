#!/bin/sh

status=$(synclient -l | grep TouchpadOff | awk '{ print $3 }')

if [ $1 = "toggle" ]; then
    if [ $status -eq 0 ]; then
        synclient TouchpadOff=1
    else
        synclient TouchpadOff=0
    fi
fi

